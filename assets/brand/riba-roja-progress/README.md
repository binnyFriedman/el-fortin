# Riba-Roja construction progress photos

## Placeholders (current)

`placeholder-01.jpg` through `placeholder-03.jpg` are illustrative stock images until on-site photography is available. Set `PROGRESS_PLACEHOLDER` to `false` in `assets/js/site.js` when swapping to real photos.

## Real site photos

Add site progress images here (JPG or WebP, e.g. `2026-06-site-01.jpg`).

Then update the `PROGRESS_IMAGES` array in `assets/js/site.js` (portfolio-brochure-sales and portfolio-brochure pages).
